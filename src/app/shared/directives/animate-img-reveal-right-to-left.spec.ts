import { AnimateImgRevealRightToLeft } from './animate-img-reveal-right-to-left';

describe('AnimateImgRevealRightToLeft', () => {
  it('should create an instance', () => {
    const directive = new AnimateImgRevealRightToLeft();
    expect(directive).toBeTruthy();
  });
});
