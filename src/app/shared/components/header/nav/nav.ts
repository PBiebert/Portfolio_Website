import { Component } from '@angular/core';
import { BtnSwitch } from '../../buttons/btn-switch/btn-switch';

@Component({
  selector: 'app-nav',
  imports: [BtnSwitch],
  templateUrl: './nav.html',
  styleUrl: './nav.scss',
})
export class Nav {}
