import { InvertCase } from './invert-case';

describe('InvertCase', () => {
  it('should create an instance', () => {
    const directive = new InvertCase();
    expect(directive).toBeTruthy();
  });
});
